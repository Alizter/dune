(cram
 (applies_to sandboxing-stale-directory-target)
 (deps %{bin:bash}))

(cram
 (applies_to interfere-sandbox-deletion)
 (deps %{bin:bash}))

(cram
 (applies_to
  invalid-junction-cleanup
  directory-junction-cleanup
  dangling-junction-cleanup
  wsl-symlink-cleanup)
 (alias runtest-windows))
