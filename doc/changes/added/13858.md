- On Windows, embed a `longPathAware` manifest in dune.exe to support paths
  longer than 260 characters. This requires the `LongPathsEnabled` Windows
  registry key to also be enabled. (#13858, @Alizter)
