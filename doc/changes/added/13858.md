- Add `(windows_manifest <file>)` field to executable stanzas to embed a
  Windows application manifest resource. On non-Windows platforms the field
  is silently ignored. Dune's own executable uses this for `longPathAware`
  support, enabling paths longer than 260 characters when the
  `LongPathsEnabled` Windows registry key is enabled. (#13858, @Alizter)
