- Portable lock directories are now produced by a single solve over all
  requested platforms instead of one solve per platform: every requested
  platform must have a solution or locking fails without writing a partial
  lock directory, and a package selected on multiple platforms must select
  the same version everywhere. (#14458, @Alizter)
