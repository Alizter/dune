- `and` and `or` in the boolean language now use short-circuit evaluation.
  Expressions are evaluated left-to-right and evaluation stops as soon as the
  result is determined. (#TODO, fixes #9943, @Alizter)
