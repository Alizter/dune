- Portable lock directories now select one package version across all requested
  platforms, and fail when no common version exists. When
  `solve_for_platforms` is omitted, Dune solves only for the detected current
  platform. (#15982, fixes #13647, @Alizter)
